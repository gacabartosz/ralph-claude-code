# fix_plan — ralph-claude-code (self-hosting / dogfood)

> ONE sub-step per loop. Pick the first `- [ ]`. Verify with `npm test` (100% green), commit + push
> to `fork`, then mark `- [x]`. Never touch the global install. See `.ralph/PROMPT.md` for guardrails.

## Phase R — Reconcile reality vs docs (DO THIS FIRST)
The docs (`IMPLEMENTATION_STATUS.md`, `IMPLEMENTATION_PLAN.md`) say v0.9.8 / Phase 1, but git is
**v0.11.5** with much of Phase 3 (and possibly Phase 6 sandbox) already merged. Do NOT implement
anything until the plan reflects reality.

- [x] R.1 — Run `npm test`; record the real current test count + pass rate (used in R.3). Read-only step.
  - **Result (2026-06-22, on macOS host: bash 3.2.57 + BSD coreutils):** 691 tests planned; 683 executed → **679 pass / 4 fail**; 8 not executed (test_monitor.bats setup errors out).
  - **All 12 anomalies are HOST-specific, NOT code regressions** — they pass on the Linux CI quality gate (bash 4+/5+, GNU coreutils):
    1. `test_log_rotation.bats` "BSD stat fallback" — the stub delegates to GNU `stat -c%s`, absent on BSD `stat`.
    2. `test_notifications.bats` "notify-send on Linux" — macOS ships `/usr/bin/osascript`; test keeps `/usr/bin` in PATH so it can't hide it.
    3. `test_ralph_enable.bats` ×2 (`.ralphrc` verification) — `source <(process-substitution)` does not define functions in bash 3.2.57.
    4. `test_monitor.bats` ×8 (0 run) — setup uses `head -n -1` (GNU-only; BSD `head` errors) + bash-3.2 process substitution.
  - **NOTE for R.4:** add a genuinely-open item "make bats suite macOS-portable" (replace `head -n -1` with `sed '$d'`, gate BSD-stat/osascript stubs, avoid `source <(...)` for function defs).
- [x] R.2 — Build the true open-work list: `gh issue list --state open` + `git log`; cross-check vs `IMPLEMENTATION_STATUS.md`. **Result (2026-06-22):**
  - **`IMPLEMENTATION_STATUS.md` is badly stale** — claims v0.9.8 / Phase 1 in progress / 276 tests / 11 files. Reality: **v0.11.5, 691 tests / 24 files** (R.1). Does not mention the multi-provider epic at all. → R.3 rewrites it.
  - **Confirmed DONE in code** (grep + closed upstream issues): `rotate_logs` (#18/#236), `track_metrics` (#21), `send_notification` (#22/#240), `create_backup` (#23/#241), `MAX_TOKENS_PER_HOUR` (#223), sandbox docker/e2b (`.ralphrc` config + ralph_loop.sh handling; #74/#75/#78).
  - **ALL phases in THIS fix_plan are already CLOSED upstream → stale, remove in R.4:** Phase 1.5 `#51` CLOSED; Phase 2 `#32–#35` CLOSED; Phase 5 `#69/#71/#72/#73` CLOSED; Phase 6 `#74/#75/#78` CLOSED.
  - **TRUE open work (upstream OPEN issues):**
    - **Multi-provider epic — the new dominant roadmap (UNSTARTED: `lib/agents/` absent, `AGENT_PROVIDER` unimplemented):** `#312` P1.1 command-construction seam (`lib/agents/` registry + Claude ref adapter), `#313` P1.2 output-normalization seam, `#314` P1.3 provider selection (`AGENT_PROVIDER` config + CLI + tmux), `#315` P2.1 capabilities matrix + degradation, `#316` P2.2 generic adapter test harness (mock CLIs), `#317` P3.1 Codex adapter pilot, `#318` P4.1 Gemini, `#319` P4.2 OpenCode, `#320` P4.3 Droid, `#321` P4.4 Kilocode, `#322` P4.5 Copilot, `#323` P5.1 sandbox provider-awareness, `#324` P5.2 monitor/status.json provider surfacing, `#325` P5.3 docs sweep, `#327` P8.1 recompile triage lock (CI).
    - **Standalone enhancements:** `#213` KEEP_MONITOR_AFTER_EXIT, `#163` monorepo-aware, `#157` Nix flake, `#156` Windows support, `#138` automate version/test badges, `#110` token cost tracking, `#102` Plan Limit Exhaustion.
    - **From R.1:** make the bats suite macOS-portable.
- [x] R.3 — Rewrote `IMPLEMENTATION_STATUS.md` to v0.11.5: date 2026-06-22, real count **691 tests** (523 unit + 168 integration, 29 files), marked original Phases 1–6 complete (#18/#19/#20/#21/#22/#23/#223 Phase 3; #51 Phase 1.5; #32–35 Phase 2; #69/71/72/73 Phase 5; #74/75/78 Phase 6), added the multi-provider epic (#312–327) + standalone enhancements as the active roadmap, and noted the macOS host caveat. Committed `docs(status): reconcile to v0.11.5`.
- [x] R.4 — Refreshed the open-work list from real upstream issues. **Result (2026-06-22, verified by grep before rewrite):**
  - **Removed (verified DONE in code, CLOSED upstream):** old Phase 1.5 `#51` session expiration (`SESSION_EXPIRATION_SECONDS=86400` + expiry check live in `lib/response_analyzer.sh:869-958`); old Phase 2 `#32–35` Agent SDK (superseded by the multi-provider epic below); old Phase 5 `#69/71/72/73` GitHub issue integration (`lib/task_sources.sh` has beads/GitHub/PRD import); old Phase 6 `#74/75/78` sandbox (`SANDBOX_PROVIDER` docker/e2b config in `.ralphrc:59-74` + ralph_loop.sh handling).
  - **Kept as genuinely OPEN** (verified unstarted: `lib/agents/` ABSENT, `AGENT_PROVIDER` unreferenced): multi-provider epic `#312–327`, standalone enhancements `#213/163/157/156/138/110/102`, and the R.1 macOS-portability fix. Listed below as granular sub-steps.
  - Committed `docs(plan): refresh open work from real issues`.

## Phase P0 — Self-hosting hygiene (quick win; do FIRST)
- [x] P0.a — Made the bats suite macOS-portable; dogfood host now runs **691/691 green** (was 679 pass / 4 fail / 8 not-run on bash 3.2 + BSD coreutils). **Fixes (no assertions weakened):**
  - `test_monitor.bats`: `head -n -1` → `sed '$d'` (BSD-portable) AND switched both sourcing sites from `source <(...)` to a temp-file source — root cause of the "8 not-run" was bash 3.2 silently defining **zero** functions from a process-substitution source (`display_status` missing → every test errored in setup).
  - `test_log_rotation.bats`: BSD-`stat` fallback test's stub returned the byte count via `wc -c` instead of delegating to GNU `stat -c%s` (which doesn't exist on real BSD `stat`) — keeps the test meaningful on both platforms.
  - `test_notifications.bats`: "notify-send on Linux" test now restricts PATH to the mock dir during the call so a host `osascript` (`/usr/bin/osascript` on macOS) can't shadow the notify-send branch; reads the call file via the `$(<…)` builtin.
  - `test_ralph_enable.bats` ×2: extract `phase_verification` to a temp file and source it instead of `source <(sed …)` (bash 3.2 process-substitution function-def issue).

## Phase MP — Multi-provider epic (dominant roadmap, UNSTARTED; `#312–327`)
> Big items: split further into sub-steps when picked (scope ≤6 files/loop). Each ships bats tests; `npm test` stays green.
- [x] MP.1 — `#312` P1.1 command-construction seam DONE. Created `lib/agents/registry.sh` (`agent_build_command` dispatch + `AGENT_PROVIDER` default=claude, sourced after the `_env_AGENT_PROVIDER` capture per the capture-before-source convention) and `lib/agents/claude.sh` (`agent_claude_build_command` reference adapter). `build_claude_command()` in `ralph_loop.sh` is now a thin wrapper delegating to `agent_build_command "$@"`; `CLAUDE_CMD_ARGS` output interface and argv unchanged. New `tests/unit/test_agents_registry.bats` (19 golden-args tests incl. model/effort/tools/resume ordering, multiline prompt, unknown-provider failure, and the no-`--dangerously-skip-permissions` security invariant). Full suite **710 green**. Selection still hardcoded to claude (config/CLI wiring is MP.3 #314).
- [x] MP.2 — `#313` P1.2 output-normalization seam DONE. Moved the Claude-specific parsing (`detect_output_format`, `parse_json_response`, `_file_size_bytes`, `RALPH_JSONL_SAFE_MAX_BYTES`) wholesale from `lib/response_analyzer.sh` into `lib/agents/claude.sh` (mechanical `sed` move — no transcription) and added contract wrappers `agent_claude_detect_format` / `agent_claude_normalize_response`. `response_analyzer.sh` now sources `claude.sh` and `analyze_response()` routes the two call sites through those wrappers; the `$output_format` raw-detect value is preserved so the Issue #224 "json detected but parse failed → suppress heuristics" path is unchanged. Added `agent_detect_format` / `agent_normalize_response` dispatch to `registry.sh` (symmetry; runtime/MP.3). New `tests/unit/test_claude_adapter.bats` (15 regression fixtures: flat / CLI object / array-stream session-id fallback / permission denials / text fallback / JSONL size guard / explicit EXIT_SIGNAL precedence). Full suite **725 green**; existing json_parsing (56) + exit_detection (54) + session_continuity (26) unchanged. Sourced `claude.sh` directly (not the registry) to avoid `AGENT_PROVIDER` source-time pollution of the capture-before-source convention.
- [x] MP.3 — `#314` P1.3 provider selection DONE. `--provider <name>` CLI flag (validated at parse time against `AGENT_REGISTERED_PROVIDERS`), `AGENT_PROVIDER` resolved in `main()` with precedence **env > CLI > .ralphrc > default(claude)** (`_env_AGENT_PROVIDER` highest, `.ralphrc` sourced lowest), unknown-provider validation with guidance, `--help` documents the flag + lists registered providers, tmux `--monitor` forwards `--provider` (env/.ralphrc inherited by the pane so only the CLI choice is forwarded), and `lib/enable_core.sh` `generate_ralphrc` emits `AGENT_PROVIDER="claude"`. Added `agent_provider_is_registered()` + `AGENT_REGISTERED_PROVIDERS` to `registry.sh`. **Bats:** 10 new in `test_cli_parsing.bats` (parse, unknown→exit+guidance, missing-arg, help lists providers, default=claude, env-rejected, .ralphrc-rejected, env>.ralphrc, forwarding present/absent) + 1 in `test_enable_core.bats`. Full suite **736 green**.
- [x] MP.4a — `#315` P2.1 capabilities matrix INFRASTRUCTURE DONE (split from MP.4; pure addition, zero behavior change). `lib/agents/claude.sh` declares `AGENT_CLAUDE_CAPABILITIES` (`supports_token_usage`, `supports_permission_denials`, `supports_api_limit_detection`, `supports_session_resume`) + `agent_claude_has_capability`. `lib/agents/registry.sh` adds `agent_has_capability` dispatch (unknown providers support nothing) and `agent_capability_enabled <cap> [label]` — returns 0 if supported, else logs a **one-time** WARN per capability and returns 1 so callers can no-op. **Bats:** 8 new in `test_agents_registry.bats` (claude full set, unknown-cap reject, dispatch, unknown-provider=nothing, gate pass/no-warn, gate fail+warn, one-time-warn, distinct-caps-each-warn). Full suite **744 green**.
- [x] MP.4b — `#315` P2.1 capability gates APPLIED in `ralph_loop.sh` DONE (Claude=all-true ⇒ zero behavior change; verified by the full suite staying green). Gated: `MAX_TOKENS_PER_HOUR` token limiting in `can_make_call` on `supports_token_usage`; permission-denial circuit breaker (#101) in `should_exit_gracefully` on `supports_permission_denials`; API-limit detection Layers 2-4 (#100/#183) in `execute_claude_code` on `supports_api_limit_detection`; session continuity in `main()` on `supports_session_resume` (disables `CLAUDE_USE_CONTINUE` when unsupported). Each unsupported gate no-ops + logs a one-time WARN via `agent_capability_enabled`. Exit detection via `RALPH_STATUS` stays always-on (ungated). **Bats:** new `test_capability_gates.bats` (7) — token + permission gates tested against the REAL extracted functions (awk extraction; BSD-sed `{`-address bug avoided) for both claude (active) and an unsupported provider (no-op + one-time WARN); API-limit + session gates verified by wiring assertions (live in non-extractable `execute_claude_code`/`main`). Full suite **751 green**.
- [x] MP.5 — `#316` P2.2 generic adapter test harness DONE. New `tests/helpers/adapter_harness.bash` — adapter-agnostic helpers that take an adapter's function names/record: `harness_assert_argv` (command-build argv), `harness_normalize` + `harness_assert_field` (recorded fixture → internal struct), `harness_assert_capabilities_wellformed` (non-empty, all `supports_*`), and `harness_make_mock_cli` (executable stub that sleeps >1s per the e2e early-failure rule and emits canned output). New `tests/fixtures/adapters/` raw provider outputs (`claude_cli_object.json`, `claude_stream_array.json`). **Bats:** new `test_adapter_harness.bats` (9) — drives the Claude reference adapter through all three checks AND a throwaway mock adapter end-to-end (build+normalize+capabilities), exercises the mock-CLI generator (executable, emits fixture, >1s), and rejects empty/malformed capability records. Ready for #317+ provider PRs to plug in. Full suite **760 green**.
- [x] MP.6a — `#317` P3.1 Codex adapter CORE DONE (split from MP.6). New `lib/agents/codex.sh` implements the full contract: `agent_codex_build_command` (`codex exec [resume <id>] --json [-m <model>] <prompt>`; reuses `CLAUDE_MODEL`/`CLAUDE_USE_CONTINUE`; prepends loop context; never emits `--dangerously-bypass`), `agent_codex_detect_format` (JSONL), `agent_codex_normalize_response` (maps Codex JSONL events → Ralph's internal struct; RALPH_STATUS in assistant text drives exit detection), and capabilities `supports_token_usage supports_session_resume` (NOT permission_denials/api_limit → degrade per #315). `registry.sh` registers `codex` (`AGENT_REGISTERED_PROVIDERS="claude codex"`) + codex cases in all 4 dispatchers. New fixture `tests/fixtures/adapters/codex_jsonl.json`. **Bats:** new `test_codex_adapter.bats` (15) via the #316 harness — build argv (basic/cmd-override/model/resume/context/missing/security), JSONL normalize (struct/summary/errors→is_stuck/detect), capabilities, and registry dispatch. Full suite **775 green**.
- [x] MP.6b — `#317` P3.1 Codex loop INTEGRATION DONE. `response_analyzer.sh` now sources the registry and `analyze_response()` routes detection + normalization through the dispatch (`agent_detect_format` / `agent_normalize_response`) instead of the hardcoded `agent_claude_*` wrappers, so `ralph --provider codex` analyzes Codex JSONL end-to-end (exit detection via `RALPH_STATUS` confirmed). Resolved the source-time ordering by moving the `_env_AGENT_PROVIDER` capture to the very top of `ralph_loop.sh` (before any lib source, since the analyzer now pulls in the registry transitively) — capture-before-source preserved. Claude path byte-identical (dispatch routes claude→`agent_claude_*`). **Bats:** 2 new analyzer-dispatch integration tests in `test_codex_adapter.bats` (codex JSONL → exit via RALPH_STATUS; no RALPH_STATUS → no exit, #224 suppression). Full suite **777 green**.
- [x] MP.6c — `#317` P3.1 Codex DOCS + E2E DONE → completes #317. New `docs/providers/CODEX.md` (requirements, enabling/precedence, command shape, JSONL event→struct mapping, capabilities+degradation table, knobs, pilot limitations); README "Multi-Provider Support (experimental)" section with a provider-capability matrix (claude=full, codex=pilot) + link to the doc; `lib/enable_core.sh` `.ralphrc` comment now lists `codex` as an option. **Bats:** new `tests/integration/test_codex_e2e.bats` (3) drives the adapter against a `harness_make_mock_cli` codex stub — build (dispatch)→run mock binary→capture JSONL→detect+normalize→exit via RALPH_STATUS; full pipeline via `analyze_response`; resume forwards the session id. Full suite **780 green** (one tmux integration test is flaky under full-suite load; passes in isolation + on re-run). `.ralphrc` itself is a protected file, so Codex notes live in the generator + docs.
- [x] MP.7a — `#318` P4.1 Gemini adapter CORE DONE. `lib/agents/gemini.sh` implements the full contract: build `gemini -o json [-m <model>] [--approval-mode <mode>] [--session-id <id> -r] -p <prompt>` (race-free pre-assigned session + resume; never emits `--approval-mode yolo`; prepends loop context; reuses `CLAUDE_MODEL`/`CLAUDE_USE_CONTINUE`/`CLAUDE_OUTPUT_FORMAT`/`GEMINI_CMD`/`GEMINI_APPROVAL_MODE`), `agent_gemini_detect_format`, `agent_gemini_normalize_response` (Gemini `-o json` object → internal struct; RALPH_STATUS drives exit), capabilities `supports_token_usage supports_session_resume`. Registered in `registry.sh` (`AGENT_REGISTERED_PROVIDERS="claude codex gemini"` + 4 dispatch cases). New fixture `gemini_json.json` + `tests/unit/test_gemini_adapter.bats` (17) via the #316 harness (build incl. preassign/resume + yolo-refusal security, normalize, capabilities, dispatch). Because the analyzer routes through the dispatch (MP.6b), `ralph --provider gemini` works end-to-end on registration. Full suite **797 green**.
- [x] MP.7b — `#318` P4.1 Gemini DOCS + E2E DONE → completes #318. New `docs/providers/GEMINI.md` (requirements, enabling/precedence, command shape, `-o json` object→struct mapping, capabilities+degradation table incl. `--approval-mode`/no-yolo, knobs, limitations); README provider matrix gains a `gemini` row + doc link; `lib/enable_core.sh` `.ralphrc` comment lists `gemini` and points at the provider docs. **Bats:** new `tests/integration/test_gemini_e2e.bats` (3) drives the adapter against a `harness_make_mock_cli` gemini stub — build (dispatch)→run mock binary→capture JSON→detect+normalize→exit via RALPH_STATUS; full pipeline via `analyze_response`; pre-assigned session id forwarded. Full suite **800 green**.
- [x] MP.8a — `#319` P4.2 OpenCode adapter CORE DONE. `lib/agents/opencode.sh` implements the full contract: build `opencode run --format json [-m <provider/model>] [-s <session-id>] <prompt>` (prompt positional; resume via `-s`; **never** emits `--dangerously-skip-permissions` despite the issue listing it — cross-adapter security invariant; prepends loop context; reuses `CLAUDE_MODEL`/`CLAUDE_USE_CONTINUE`/`CLAUDE_OUTPUT_FORMAT`/`OPENCODE_CMD`), `agent_opencode_detect_format` (JSONL), `agent_opencode_normalize_response` (OpenCode JSONL events → internal struct; RALPH_STATUS drives exit), capabilities `supports_token_usage supports_session_resume`. Registered in `registry.sh` (`AGENT_REGISTERED_PROVIDERS="claude codex gemini opencode"` + 4 dispatch cases). New fixture `opencode_jsonl.json` + `tests/unit/test_opencode_adapter.bats` (16) via the #316 harness (build incl. resume + skip-permissions-refusal security, normalize, capabilities, dispatch). Works end-to-end via the analyzer dispatch. Full suite **816 green**.
- [x] MP.8b — `#319` P4.2 OpenCode DOCS + E2E DONE → completes #319. New `docs/providers/OPENCODE.md` (requirements, enabling/precedence, command shape incl. positional prompt + no-skip-permissions security note, JSONL event→struct mapping, capabilities+degradation table, knobs, limitations); README provider matrix gains an `opencode` row + doc link; `lib/enable_core.sh` `.ralphrc` comment lists `opencode`. **Bats:** new `tests/integration/test_opencode_e2e.bats` (3) drives the adapter against a `harness_make_mock_cli` opencode stub — build (dispatch)→run mock binary→capture JSONL→detect+normalize→exit via RALPH_STATUS; full pipeline via `analyze_response`; `-s` resume forwards the session id. Full suite **819 green**.
- [x] MP.9a — `#320` P4.3 Droid (Factory Droid) adapter CORE DONE. `lib/agents/droid.sh` implements the full contract: build `droid exec [-o json] [-m <model>] [--session-id <id>] [--auto <level>] <prompt>` (prompt positional; resume via `--session-id`; prepends loop context; reuses `CLAUDE_MODEL`/`CLAUDE_USE_CONTINUE`/`CLAUDE_OUTPUT_FORMAT`/`DROID_CMD`/`DROID_AUTO`; omits `-m` when `CLAUDE_MODEL` empty so Droid uses its default claude-opus-4-8), `agent_droid_detect_format`, `agent_droid_normalize_response` (Droid `-o json` object `.result`/`.session_id` → internal struct; RALPH_STATUS drives exit), capabilities `supports_token_usage supports_session_resume`. Registered in `registry.sh` (`AGENT_REGISTERED_PROVIDERS="claude codex gemini opencode droid"` + 4 dispatch cases). New fixture `droid_json.json` + `tests/unit/test_droid_adapter.bats` (17) via the #316 harness (build incl. resume/`--auto`/default-model-omits-`-m`, normalize, capabilities, dispatch). Works end-to-end via the analyzer dispatch. Full suite **836 green**.
- [x] MP.9b — `#320` P4.3 Droid DOCS + E2E DONE → completes #320. New `docs/providers/DROID.md` (requirements, enabling/precedence, command shape incl. positional prompt + default-model `claude-opus-4-8` + `--auto`, JSON object→struct mapping, capabilities+degradation table, knobs, limitations); README provider matrix gains a `droid` row + doc link; `lib/enable_core.sh` `.ralphrc` comment lists `droid`. **Bats:** new `tests/integration/test_droid_e2e.bats` (3) drives the adapter against a `harness_make_mock_cli` droid stub — build (dispatch)→run mock binary→capture JSON→detect+normalize→exit via RALPH_STATUS; full pipeline via `analyze_response`; `--session-id` resume forwarded. Full suite **839 green**.
- [x] MP.10a — `#321` P4.4 Kilocode adapter CORE DONE. `lib/agents/kilocode.sh` implements the full contract: build `kilocode --auto [-j] [-mo <model>] [-c] <prompt>` (`--auto` always present — headless requirement; `-j` structured output needs `--auto`; `-mo` model knob — NOT `-m`; `-c` continue-LAST-only when `CLAUDE_USE_CONTINUE=true` + a prior session id exists — no session-id targeting, the weakest resume form; prompt positional with loop context prepended; **never** emits `--yolo` — cross-adapter security invariant since `--auto` already gives headless autonomy; reuses `CLAUDE_MODEL`/`CLAUDE_USE_CONTINUE`/`CLAUDE_OUTPUT_FORMAT`/`KILOCODE_CMD`), `agent_kilocode_detect_format`, `agent_kilocode_normalize_response` (Kilocode `-j` JSON object `.result`/`.session_id` → internal struct; RALPH_STATUS drives exit), capabilities `supports_token_usage supports_session_resume` (continue-last caveat documented; NOT permission_denials/api_limit → degrade per #315). Registered in `registry.sh` (`AGENT_REGISTERED_PROVIDERS="claude codex gemini opencode droid kilocode"` + 4 dispatch cases). New fixture `kilocode_json.json` + `tests/unit/test_kilocode_adapter.bats` (17) via the #316 harness (build incl. continue-last/-mo/no-yolo security/text-omits-j, normalize, capabilities, dispatch). Works end-to-end via the analyzer dispatch. Full suite **856 green**.
- [x] MP.10b — `#321` P4.4 Kilocode DOCS + E2E DONE → completes #321. New `docs/providers/KILOCODE.md` (requirements, enabling/precedence, command shape incl. `--auto`/`-j`/`-mo`/continue-last + no-yolo security note, JSON object→struct mapping, capabilities+degradation table with a dedicated continue-last session caveat section, knobs, limitations); README provider matrix gains a `kilocode` row (`⚠️ -c continue-last†`) + footnote + doc link, intro+one-off comment list kilocode; `lib/enable_core.sh` `.ralphrc` comment lists `kilocode` and points at `{…,KILOCODE}.md`. **Bats:** new `tests/integration/test_kilocode_e2e.bats` (3) drives the adapter against a `harness_make_mock_cli` kilocode stub — build (dispatch)→run mock binary→capture JSON→detect+normalize→exit via RALPH_STATUS; full pipeline via `analyze_response`; `-c` continue-last forwarded. Full suite **859 green**.
- [x] MP.11a — `#322` P4.5 Copilot adapter CORE DONE (the DEGRADED, TEXT-ONLY case — validates #315 graceful degradation under the hardest provider). `lib/agents/copilot.sh` implements the full contract: build `copilot -s [--model <m>] [--resume <id>] [--allow-tool <t>]… [--deny-tool <t>]… -p <prompt>` (`-s/--silent` always; prompt via `-p` flag with loop context prepended; `--model` from shared `CLAUDE_MODEL`; `--resume <id>` when `CLAUDE_USE_CONTINUE=true` + prior session; granular `--allow-tool`/`--deny-tool` from `COPILOT_ALLOWED_TOOLS`/`COPILOT_DENIED_TOOLS`; **never** emits `--allow-all` — cross-adapter security invariant, grant specific tools instead; `CLAUDE_OUTPUT_FORMAT` ignored — Copilot has no JSON), `agent_copilot_detect_format` (**always "text"** → routes `analyze_response` down its RALPH_STATUS text-mode path #224), `agent_copilot_normalize_response` (TEXT parse → internal struct; RALPH_STATUS drives exit; no session/usage/error fields in text), capabilities **`supports_session_resume` ONLY** (no token_usage/permission_denials/api_limit → all degrade per #315). Registered in `registry.sh` (`AGENT_REGISTERED_PROVIDERS="…kilocode copilot"` + 4 dispatch cases). New fixture `copilot_text.txt` + `tests/unit/test_copilot_adapter.bats` (18) via the #316 harness (build incl. --model/--resume/allow-tool/deny-tool/no-allow-all security/text-only-no-json-flag, always-text detect, text normalize incl. no-RALPH_STATUS=no-exit, minimal capabilities, dispatch). Works end-to-end via the analyzer text path. Full suite **877 green**.
- [x] MP.11b — `#322` P4.5 Copilot DOCS + E2E DONE → completes #322. New `docs/providers/COPILOT.md` (calls out the reduced/text-only feature set up front; command shape incl. `-s`/`-p`/granular `--allow-tool`/`--deny-tool` + no-`--allow-all` security note + `CLAUDE_OUTPUT_FORMAT` ignored; TEXT→struct mapping via the #224 text path; capabilities+degradation table with token-limit/perm-breaker/api-limit all OFF and session-resume ON; knobs; limitations); README provider matrix gains a `copilot` row (`text-only‡`, token-limiting ❌) + a ‡ footnote + doc link, intro+one-off comment list copilot; `lib/enable_core.sh` `.ralphrc` comment lists `copilot (text-only/degraded)` and points at `{…,COPILOT}.md`. **Bats:** new `tests/integration/test_copilot_e2e.bats` (3) drives the adapter against a `harness_make_mock_cli` copilot stub — build (dispatch)→run mock binary→capture TEXT→detect="text"→normalize exit via RALPH_STATUS; full pipeline via `analyze_response` (text path, `output_format=text`); `--resume` forwarded + degraded capability gates (`supports_token_usage`/`supports_api_limit_detection`) warn-and-no-op. Full suite **880 green**.
- [-] MP.12 — `#323` P5.1 sandbox provider-awareness **[BLOCKED: needs human]**. The premise (`lib/sandbox_docker.sh`/`lib/sandbox_e2b.sh` "currently wrap the Claude CLI call") is **false in this repo**: there is NO sandbox execution layer to generalize. Verified (Loop 64): no `lib/sandbox_*.sh`, no `--sandbox` CLI flag, no Dockerfile, and `grep -rn "SANDBOX_" --include="*.sh"` returns nothing — the `.ralphrc` `SANDBOX_PROVIDER`/`SANDBOX_DOCKER_*`/`SANDBOX_E2B_*` block (lines 59–80) is **placeholder config read by zero shell code**. The command is executed directly via `portable_timeout … "${CLAUDE_CMD_ARGS[@]}"` (`ralph_loop.sh:1572` live / `:1656` background) with no sandbox branch; line 1310 only *mentions* "sandbox wrapping" as a hypothetical downstream consumer. The fix_plan Notes line 21 + `IMPLEMENTATION_STATUS.md:114` claim sandbox #74/#75/#78 is "DONE in code" — that claim is INACCURATE (config/docs only). **To unblock:** a human must first decide whether to build the sandbox execution layer (#74/#75/#78: docker+e2b wrappers, `--sandbox` flag parsing, default `ralph-sandbox` Dockerfile, per-provider credential seeding). Once it exists, provider-awareness at the *command-wrap* level is largely free — it wraps `${CLAUDE_CMD_ARGS[@]}`, which already holds the **active provider's** argv via the #312 registry seam — leaving only per-provider credential/auth seeding + custom images as real work. Marked `[-]` (not `[ ]`) so the loop advances to MP.13; flip back to `[ ]` after the sandbox layer lands.
- [x] MP.13a — `#324` P5.2 status.json provider field DONE. `update_status()` in `ralph_loop.sh` now writes `"provider": "${AGENT_PROVIDER:-claude}"` into `.ralph/status.json` (top-level, after `timestamp`); the `:-claude` fallback keeps it correct even before `main()` resolves the provider. **Bats:** 2 new in `test_status_updates.bats` (defaults to claude when unset; surfaces an explicit `AGENT_PROVIDER=codex`). Full suite **882 green**.
- [x] MP.13b — `#324` P5.2 monitor provider display DONE → completes #324. `ralph_monitor.sh` `display_status()` reads `.provider // "claude"` from `status.json` and renders a `Provider:` line in the "Current Status" box (between Loop Count and Status); the `// "claude"` jq fallback + `|| echo claude` guard keep it correct for older status files and corrupted JSON (matching the existing field-read pattern). **Bats:** 2 new in `tests/integration/test_monitor.bats` (renders an explicit `provider:"codex"`; falls back to `claude` when the field is absent). Full suite **884 green**.
- [x] MP.14a — `#325` P5.3 docs sweep: CLAUDE.md DONE. Added `kilocode.sh` (#321) and `copilot.sh` (#322, the degraded text-only case) adapter bullets to the `lib/agents/` section (matching the codex/gemini/opencode/droid entries — build shape, normalize, capabilities, registration, docs+E2E), and a **Provider surfacing (#324, MP.13)** bullet documenting the `status.json` `provider` field + `ralph_monitor.sh` `Provider:` display. README provider matrix + `--provider` usage + per-provider docs links + `enable_core.sh` `.ralphrc` note were already completed in MP.6c–MP.11b. Docs-only (no test references CLAUDE.md); suite unchanged at **884 green**.
- [x] MP.14b — `#325` P5.3 docs sweep: templates + consistency DONE → completes #325. Added an `AGENT_PROVIDER` section (claude default; lists all 7 providers incl. copilot text-only; precedence + `docs/providers/{…}.md` pointer) to **`templates/ralphrc.template`** AND the **`setup.sh` inline `.ralphrc` fallback** heredoc, matching the `lib/enable_core.sh`-generated `.ralphrc` (the primary path setup.sh uses, already provider-aware). Consistency verified: all 6 `docs/providers/*.md` (CODEX/GEMINI/OPENCODE/DROID/KILOCODE/COPILOT) are linked from the README provider section; README matrix + `--provider` usage + CLAUDE.md `lib/agents/` section (MP.14a) cover all shipped behavior — no stale claude-only assumptions remain in user docs. **Bats:** docs/config-only; targeted `test_project_setup.bats` (50) + `test_installation.bats` (65) green, full suite **884 green** (no test count change).
- [x] MP.15a — registry/adapter parity guard DONE (the fix_plan's stated MP.15 deliverable: "a test asserting registry/adapter parity"). New `tests/unit/test_registry_parity.bats` (7) asserts the invariants that keep `ralph --provider <name>` working for EVERY registered provider: ≥1 provider incl. claude; each `AGENT_REGISTERED_PROVIDERS` entry defines all 4 contract functions (`agent_<p>_{build_command,detect_format,normalize_response,has_capability}`); each declares a well-formed `AGENT_<UPPER>_CAPABILITIES` record (via `harness_assert_capabilities_wellformed`); `has_capability` dispatch agrees with the adapter's own fn (no unknown-provider fallthrough) + rejects bogus caps; build dispatch yields non-empty argv for every provider; **no orphan adapters** (every `lib/agents/*.sh` except registry is registered); unknown provider rejected by validation + dispatch. Catches the common drift (register without functions, ship adapter without registering, forget a capability record). Full suite **891 green**.
- [-] MP.15b — `#327` P8.1 gh-aw lock recompile **[BLOCKED: needs human + toolchain]**. The REAL upstream #327 is unrelated to adapters (fix_plan's "adapters stay in sync" description was inaccurate): it's "Recompile `.github/workflows/triage-incoming-issues.lock.yml` to pick up gh-aw v0.79.8 + checkout v6.0.3". Requires installing the `gh aw` extension at **v0.79.8** (NOT installed here — `gh extension list` shows no gh-aw) and running `gh aw compile` to regenerate the compiled CI artifact; **hand-editing the `setup` pin is explicitly forbidden** by the issue and its guard (#287/PR #283). The referenced guard `test_workflow_sha_pinning.bats` (upstream "not ok 995") is **not part of this repo's `npm test`** (`bats tests/unit tests/integration` — verified absent), so the local suite stays green regardless. Installing a specific gh-extension version from the network + recompiling a CI lock is a maintainer toolchain task outside the dogfood "edit bash + bats" scope. **To unblock:** human runs `gh extension upgrade aw` (→v0.79.8) → `gh aw compile` → commit the recompiled `.lock.yml` (may trip the secret scanner on the manifest line — verify no real secrets). Marked `[-]` so the loop advances past it.

## Phase ENH — Standalone enhancements (independent; pick by priority)
- [x] ENH.1 — `#213` `KEEP_MONITOR_AFTER_EXIT` option DONE. New config knob (default `false`) + `--keep-monitor` CLI flag that, when true, skips the `tmux kill-session` chain in `setup_tmux_session()` so the monitor + live-log panes stay up for post-run review (the Ralph pane prints a "Monitor panes kept alive… close with tmux kill-session" hint instead); default preserves the #176 auto-teardown. Wired with the full precedence pattern: `_env_KEEP_MONITOR_AFTER_EXIT` capture (top) → default → `.ralphrc` re-apply in `load_ralphrc` → `_cli_KEEP_MONITOR_AFTER_EXIT` re-apply over `.ralphrc` (env > CLI > .ralphrc > default). Documented in `--help`, CLAUDE.md config list, and `templates/ralphrc.template`. **Bats:** 3 in `test_cli_parsing.bats` (flag accepted/not-unknown; listed in `--help`; combines with `--monitor`) + 2 in `test_tmux_integration.bats` (default chains `kill-session … 2>/dev/null`; `KEEP_MONITOR_AFTER_EXIT=true` skips it + prints the hint) — the inline `setup_tmux_session` fixture was updated to mirror the real conditional. Full suite **896 green**.
- [x] ENH.2a — `#110` token cost calc library DONE. New `lib/cost_utils.sh` (pure bash 3.2 + awk, no floats/assoc-arrays): `get_model_cost_rates <model>` resolves per-MTok input/output rates by model-name substring (opus 15/75, sonnet 3/15, haiku 1/5; case-insensitive) with precedence **explicit `COST_*_PER_MTOK` override > known model > configurable `COST_DEFAULT_*` (sonnet-class)**; `compute_cost <model> <in_tok> <out_tok>` prices input/output **separately** (the key correctness point — output ≈5× input) → USD 6dp, non-numeric tokens coerced to 0; `format_cost_usd` (4dp `$` display) + `add_cost` (6dp accumulation) helpers. Rates documented as approximate list prices, overridable for accuracy/unknown models. **Bats:** new `tests/unit/test_cost_utils.bats` (18) — rate resolution incl. overrides/case/default, cost math (opus 1M+1M=90, input-only, output-rate, realistic small counts, zero, garbage), display + accumulation. Full suite **914 green**.
- [x] ENH.2b — `#110` cost data layer + status.json DONE. `ralph_loop.sh` sources `lib/cost_utils.sh`; new `extract_input_output_tokens()` reads input/output **separately** (`.usage`/`.metadata.usage` shapes) — the existing `extract_token_usage` (summed total) is untouched for back-compat. `update_token_count()` now also prices the new tokens via `compute_cost "${CLAUDE_MODEL:-}"` and accumulates into a new hourly `COST_FILE` (`$RALPH_DIR/.cost_usd`) with `add_cost` (best-effort; never breaks the loop). `COST_FILE` resets with the other counters (hourly rollover + `wait_for_reset`). `update_status()` emits `"estimated_cost_usd"` into `status.json` (numeric, guarded to stay valid JSON). **Bats:** 2 in `test_status_updates.bats` (field defaults to 0; surfaces accumulated cost) + 5 in `test_rate_limiting.bats` against the REAL sourced functions (split extraction incl. metadata shape + missing file; cost accumulation opus 0.0675; additive across invocations). Full suite **921 green**.
- [x] ENH.2c — `#110` monitor cost display DONE (the headline TMUX ask). `ralph_monitor.sh` `display_status()` reads `.estimated_cost_usd // 0` and renders an `Est. Cost: $X.XXXX` line in the "Current Status" box (awk `%.4f` format; numeric-guarded with a `$0.0000` fallback for old/corrupt/missing-field status files). With ENH.2b (status.json) this satisfies the issue's "show spend in --status or the TMUX" request. **Bats:** 2 in `tests/integration/test_monitor.bats` (renders `$0.0675` from a fixture; falls back to `$0.0000` when the field is absent). Full suite **923 green**.
- [x] ENH.2d — `#110` per-loop cost in metrics/ralph-stats DONE → completes #110. New `cost_delta <before> <after>` in `lib/cost_utils.sh` (after−before; falls back to `after` on an hourly reset where after<before, mirroring the `calls_this_loop` ternary). Main loop captures `cost_before_exec`/`cost_after_exec` around `execute_claude_code` and passes the delta as a new 5th `track_metrics` arg → `"cost"` field in `metrics.jsonl` (defaults to 0, back-compat). `print_metrics_summary` (ralph_loop.sh) and `ralph-stats.sh` both add `total_cost_usd` = `(map(.cost // 0)|add)` rounded to 6dp (legacy cost-less metrics → 0). **Bats:** 4 `cost_delta` cases in `test_cost_utils.bats` (normal/reset/zero/garbage) + 4 in `test_metrics_tracking.bats` (cost field via numeric eq for jq-1.7 literal preservation; default 0; ralph-stats total 0.0775; legacy→0). Full suite **931 green**. #110 fully shipped: cost lib (2a) → loop accumulation + status.json (2b) → monitor display (2c) → metrics/ralph-stats (2d).
- [x] ENH.3 — `#138` automated version/test-count badges DONE. New `scripts/update_badges.sh` regenerates the README Version + Tests shields from source: version from a `VERSION` file else the README `**Version**: vX.Y.Z` prose line; test count = `@test` definitions under the npm-test scope (tests/unit + tests/integration). Idempotent `sed -i.bak` rewrite (BSD/GNU-portable); `--check` mode exits non-zero on drift (CI guard); functions sourceable behind a `main` guard. Ran it to fix the real drift: Tests badge **556 → 942 passing** (version stays 0.11.5). **Bats:** new `tests/unit/test_update_badges.bats` (11) — version detection (prose/VERSION/malformed/none), `@test` counting (incl. missing dir), badge rewrite + no-leftover-`.bak`, `read_badge_*`, end-to-end `main` + `--check` stale/current. Gotcha handled: fixtures assemble the `@test` marker at runtime so bats' own preprocessor doesn't mangle the heredoc. Scope is the **script + unit test** (the bats-testable deliverable); the optional GitHub Actions auto-commit workflow from #138 is CI infra left for a maintainer. Full suite **942 green**.
- [x] ENH.4a — `#102` plan-limit detection + reset-time parser DONE (the testable core). New `lib/plan_limit.sh` (pure bash 3.2 + grep/sed/tr, BSD+GNU portable): `extract_reset_time` parses the reset clock from a plan message (`9pm`/`11:30pm`/`21:00`/`3am`; lowercased so BSD sed needs no `I` flag); `plan_limit_message_present` matches plan-exhaustion wording (distinct from the pure 5-hour rolling limit); `detect_plan_limit_reset <file>` applies the same tool-result noise filter as the existing API-limit layers (no false positive on echoed file content) and echoes the parsed reset time; `format_plan_limit_message` → "Claude plan usage limit reached. Ralph will resume at <time>." (or "reset time unknown"). **Bats:** new `tests/unit/test_plan_limit.bats` (16) — reset-time forms, message matching, file detection incl. noise-filter false-positive guard + missing file, formatter. Full suite **958 green**.
- [x] ENH.4b — `#102` plan-limit handling wired into the loop DONE → completes #102. `ralph_loop.sh` sources `lib/plan_limit.sh`; `execute_claude_code` adds a **Layer 2.5** (gated on `supports_api_limit_detection`, before the generic Layer-3 5-hour text fallback so the reset-time message wins): `detect_plan_limit_reset` → stash the parsed time in `PLAN_LIMIT_RESET_FILE`, log `format_plan_limit_message`, `return 2` (existing api_limit recovery handles the pause/wait). The main loop's api_limit handler reads `PLAN_LIMIT_RESET_FILE`, appends a "Resets at <time>." resume hint to the WARN log + desktop notification + user prompt, then clears the file. **Bats:** 5 in `test_cli_modern.bats` — sources lib; layer wired (returns 2); layer precedes the 5-hour fallback (ordering); handler reads `PLAN_LIMIT_RESET_FILE` + emits "Resets at"; behavioral end-to-end ("…reset at 9pm" → resume hint "resume at 9pm"). Full suite **963 green**. #102 shipped: ENH.4a parser + ENH.4b wiring.
- [ ] ENH.5 — `#163` monorepo-aware operation (per-package `.ralph/` or scoped runs). **Bats:** detection + scoping tests.
- [ ] ENH.6 — `#157` Nix flake for reproducible install. **Bats/CI:** flake builds; smoke check.
- [ ] ENH.7 — `#156` Windows support (WSL/Git-Bash compatibility audit). **Bats:** portability guards; document scope as `[BLOCKED: needs human]` if it needs a Windows host.

## Notes
- Every feature MUST ship with bats tests and keep `npm test` 100% green (repo quality gate).
- If any item needs upstream/main/global-install changes → `[BLOCKED: needs human]`, do not attempt.

## Completed
- [x] Project enabled for Ralph (self-hosting dogfood bootstrap)
